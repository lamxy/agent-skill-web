{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // 覆寫 tsconfig.json 的 noEmit：建置必須實際輸出到 dist。
    "noEmit": false,
    "outDir": "dist",
    "rootDir": "src",
    "sourceMap": true,
    "declaration": true
  },
  "include": ["src/**/*.ts"],
  "exclude": ["test/**/*.ts"]
}
