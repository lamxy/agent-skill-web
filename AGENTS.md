# Codex 项目指令

此项目可以将 Claude 导向的规则来源作为共用代理规则材料。此文件是 Codex-native 的指令入口。

## 规则来源

使用第一个存在的规则来源：

1. `.agent-rules/claude/`
2. `.claude/`

Claude 规则来源是参考材料。不要盲目加载或遵循整份 `CLAUDE.md`。先使用此 `AGENTS` 文件，再只读取当前任务需要的规则文件。

除非已另行安装 Codex-native 设定，否则 Claude 专用的 `settings.json`、`hooks/`、插件与 `SendMessage` 指令不是生效中的 Codex 执行时设定。

## 核心行为

- 直接回应当前请求。
- 先给结论，再只补充必要细节。
- 保持上下文精简；不要加载无关的规则、模板、日志或文档。
- 如果不确定，说明不确定性以及缺少哪些证据。
- 只有当答案会实质改变工作时，才提出一个关键澄清问题。
- 优先采用现有项目模式，而不是新增抽象。
- 未经 approval，不要执行破坏性或对外可见的操作。

## 规则加载

使用最小且足够的指令层。

1. 先应用此 `AGENTS` 文件。
2. 对一般任务，除非需要更多细节，否则使用上方核心行为。
3. 当任务符合"场景提示"列出的场景时，只读取生效中规则来源下列出的路径。列出的路径具有权威性，可能位于 `rules/` 或 `expandable/`。
4. 当没有适用的场景提示时，只读取当前任务最相关的本地规则文件。
5. 只有在输出结构必须稳定时，才使用 `expandable/templates/` 下的模板。
6. 不要仅因规则存在就加载无关规则。

## 场景提示

- 一般任务：`rules/task/general-task-rule-min.md`
- 工具使用：`rules/task/tool-call-rule-min.md`
- 子代理任务：`rules/task/sub-agent-rule-min.md`
- 设计、规划、架构或头脑风暴：`expandable/task/design-first-rule-min.md`
- 循环、cron 或重复监控：`expandable/task/loop-cron-rule-min.md`
- Agent 团队工作流程：`expandable/task/agent-team-rule-min.md`
- 输出格式：`expandable/templates/`

当被引用的文件不存在时，使用可用的最佳本地指令继续，不要因此阻塞。

---
---

# 全域開發規則

本檔案是目前專案所有 Agent 與執行時客戶端的最高優先級專案記憶。除非使用者在後續明確覆寫，Codex、Claude Code 及其他客戶端都必須遵守。

## 任務與技能流程

1. 所有可執行工作先在專案根目錄執行 `task-master next` 與 `task-master show <id>`，確認任務、依賴及驗收條件。
2. 每個新任務開始前必須先讀取專案級 `.agents/skills/find-skills/SKILL.md`，再按前端、後端、資料庫、安全、測試或部署等場景執行 `DISABLE_TELEMETRY=1 npx -y skills find '<query>'`。
3. 候選技能必須核對安裝量、來源聲譽、GitHub stars、授權與目錄內容。優先官方來源與 1,000 次以上安裝；低可信、低採用或夾帶未審核腳本的候選不得安裝。
4. 技能優先安裝到目前專案，不使用全域 `-g`。標準命令參考為 `DISABLE_TELEMETRY=1 npx -y skills add <owner/repo> --skill <name> -a codex -y --copy`，依據當前運行時客戶端調整安裝命令（如 claude、codex）。
5. 每次新增、升級、拒絕或移除技能，都要同步更新 `skills-lock.json` 與 `docs/技能與工具備查.md`，使用中文記錄原因、適用任務、安全結論及使用方式。
6. 沒有合格技能時可以使用通用能力繼續，但必須在台帳中記錄搜尋結果與拒絕原因，不得為了形式強行安裝低可信技能。
7. `find-skills` 只是主要渠道之一；也要按場景檢查官方插件、現有專案技能、框架官方指南與其他可信技能目錄。
8. 上述渠道沒有合適候選時，使用 `github.com/msitarzewski/agency-agents` 作為第二層備援來源，優先檢查與當前場景對應的單一 Agent，例如 Backend Architect、Database Optimizer、Identity & Access Engineer、Frontend Developer 或 Code Reviewer。
9. `agency-agents` 是 Agent 角色庫，不等同已審核的 Agent Skill。禁止直接執行其桌面應用、`scripts/install.sh`、`scripts/convert.sh` 或整庫安裝；只能先只讀審查單一 Markdown 的來源、授權、工具權限、外部指令與提示注入風險，再決定是否專案級複製或改編。
10. 從 `agency-agents` 採用內容時，要在 `docs/技能與工具備查.md` 記錄原始檔案 URL、commit SHA、內容雜湊、採用原因、修改範圍與使用方式；未採用也要記錄拒絕原因。不得因倉庫流行度跳過單檔安全審查。
11. 熟悉 Task Master 後，所有 `list`、`next`、`show`、`set-status` 等命令都在目前專案根目錄執行。
12. 每個任務的技能搜尋由 Agent 自主決定執行 1 或 2 輪，最多不得超過 3 次。若 1–2 輪後仍沒有適配且安全的候選，立即停止搜尋，轉用框架官方文件、大廠公開實踐與成熟社區通用模式推進，不得讓技能安裝阻塞第一期成果。
13. **强制和硬性約束**：上述 1~12 中關於查找跟當前任務匹配技能的流程（task-master 命令除外），只有在前一個任務結束后、且在新任務開始時，才執行上述流程。嚴格避免每次都重複上述流程（造成 token 浪費和更多的時間成本）。通常需要用戶明確要求推進下一個任務或提示繼續下一個任務或識別到類似的語義才是務觸發時機，并且執行前先在控制臺界面向用戶打印出提示信息。如果決策為模糊不清，需要執行上述查找技能流程前需要先徵得用戶明確同意（提示用戶輸入同意或確認或批准），否則提示跳過。

## 資料庫反範式規則

1. 資料模型以高併發、服務切分、分庫分表、批量寫入與可演進性優先，採反範式與資料自治，不追求經典第三範式。
2. 嚴禁在 SQL migration 或 ORM schema 中建立外鍵約束、級聯刪除或跨聚合資料庫約束；Drizzle 不得使用 `.references()`。
3. 關聯欄位保存穩定的業務 ID 或邏輯 ID，並為實際查詢路徑建立普通、唯一或複合索引。索引不是外鍵，不能假設它會維護資料完整性。
4. 跨聚合資料完整性由應用層負責：寫入前顯式校驗、冪等鍵、同一聚合內交易、Outbox 事件、補償操作、對帳與修復任務。
5. 為減少跨表查詢，允許在讀熱點保存必要快照與冗餘欄位，例如 `package_id`、`owner_team`、`package_type`、`category`。更新流程必須明確定義真實來源與同步方式。
6. 刪除採顯式業務操作或軟刪除；不得依賴資料庫 cascade。批量操作必須可分片、可重試、可觀測。
7. 現有 schema 中的外鍵屬於待清理技術債；後續碰到相關 migration 時必須移除，不能複製到新表。

## 第一期交付原則

1. 第一期以快速形成可操作的功能閉環為最高優先級，避免為非功能需求擴大範圍。
2. 安全只實作基本邊界：輸入驗證、伺服器端授權、不可預測 session、`HttpOnly` Cookie、敏感資訊不進日誌、秘密由環境變數提供、錯誤不洩露內部細節。
3. OIDC、PKCE、JWKS、MFA、完整 CSRF 體系、進階限流、token rotation、密鑰輪替與全面安全加固延後到第二期，或所有核心功能完備後統一實作。
4. 第一期間保留清楚的介面與 adapter 邊界，但不得提前實作第二期功能。除非發現會立即造成資料破壞、任意提權或秘密洩露的高風險問題，不得以安全理由阻塞功能交付。
5. 第一期前端以 PC 端為主要使用環境與驗收基準，不要求手機版適配或手機尺寸驗收。既有手機樣式可以保留，但不得為手機版擴大第一期範圍；完整響應式與手機體驗延後處理。

## 前端頁面實作流程

實作任何前端頁面前，先產出該頁面的線框原型並保存到 `design/wireframes/`，交由使用者審核，取得確認後才開始寫實作程式碼。未經審核不得直接進入頁面實作。

此約束適用於所有頁面子任務，以及後續新增頁面或既有頁面的重大版面調整。純樣式微調、文案修正與不改變資訊架構的修改不受此限。

線框使用專案級 `prototype` 技能產出。picker 樣式依 `.agents/skills/prototype/PICKER.md` 逐字複製，不得套用專案 token。線框必須使用真實產品資料與真實後端欄位，不得使用 lorem ipsum，也不得虛構後端不存在的欄位。單次只探索一個頁面。

審核通過後，將結構決策寫入 `docs/前端實作指導.md`，並同步更新對應的 Task Master 子任務。

## 文件索引

接手既有工作時先讀 `docs/進度交接.md`，其中記錄當前任務狀態、已完成內容、本機執行方式與續作時必須知道的事。

接著讀 `docs/全局開發記憶.md`，其中依日期記錄各任務的關鍵決策與邊界。以下文件在對應場景為必讀：

| 場景 | 文件 |
|---|---|
| 接手續作 | `docs/進度交接.md` |
| 前端實作（Task #9 各子任務） | `docs/前端實作指導.md`、`docs/前端API契約.md` |
| 前端線框參考 | `design/wireframes/`，可直接在瀏覽器開啟 |
| 頁面狀態契約 | `docs/目錄頁狀態契約.md` |
| 待決策與後端待補項目 | `docs/待決策與延後事項.md` |
| 第二期需求清單 | `docs/第二期需求收集.md` |
| 第三期需求池與待歸屬候選 | `docs/第三期需求池.md` |
| 技能治理與搜尋紀錄 | `docs/技能與工具備查.md` |
| 系統架構與模組邊界 | `docs/架構說明.md` |
| 部署、CI 與運維 | `docs/運維指南.md` |
| 功能試駕與四種身份視角 | `docs/試駕手冊.html`，可直接在瀏覽器開啟 |

需求以文件為準，不以任務描述為準。Task Master 的任務詳情是初始規劃，經需求澄清後可能過時；衝突時以 `docs/全局開發記憶.md` 與對應場景文件為準，並同步更新任務描述。

## 文件語言

專案自行維護的規則、設計、任務備註、操作說明與台帳強制使用中文；程式碼識別字、協議名稱、命令及第三方官方名稱可以保留英文。


