# 第三期需求池

本文件收納**尚未定期別**的候選需求。與第二期文件的分工：`docs/第二期需求收集.md` 是已確認的第二期範圍；本文件是待決定的候選池。

期別歸屬由使用者決定。不得自行把項目在期別之間移動，也不得自行把候選項升為已確認需求。

## 需求清單

目前為空。使用者尚未指定任何項目進入第三期。

<!--
新增項目時的欄位：編號（P3-n）、需求、提出日期、來源。
格式與第二期文件一致，便於日後整批移動。
-->

| 編號 | 需求 | 提出日期 | 來源 |
|---|---|---|---|
| — | — | — | — |

## 待歸屬候選

以下為第一期進行中已識別、但尚未指定期別的項目。列在此處僅為避免遺失，**不代表已納入第三期**。使用者決定期別後，再移入上方清單或第二期文件。

### 來自第一期的既知限制

| 候選項 | 現況 | 來源 |
|---|---|---|
| Windows 原生與 macOS 原生的腳本執行驗證 | 生成腳本可下載，平台端未實際執行過。需真實多平台資源 | D-5 |
| 舊 `package_versions` 單一命令欄位的相容週期結束與清理 | 保留僅供讀取；語意含糊的多目標舊列未建立 target 紀錄，需人工補齊 | D-3 結案備註 |
| 手機版與完整響應式 | 第一期以 PC 為驗收基準，既有手機樣式保留但未擴大範圍 | AGENTS.md 第一期交付原則第 5 條 |
| 遙測欄位的法務書面核准 | 工程已完成，白名單已固定。送審範圍應為 13 欄（Task 13 新增 `script_version` 與 `options`），非原十一欄 | Task 10.8 |

### 來自延後改進的技術債

| 候選項 | 現況 | 來源 |
|---|---|---|
| comparator 對 build metadata 相異版本回傳 0 | 僅影響展示型排序，推薦版路徑已有 tie-break | R-1 |
| comparator 全序性質的直接單元測試 | 目前只被端到端測試間接覆蓋 | R-2 |
| 稽核 API 的候選值端點 | 缺事件 facet、全身份候選、依 `targetType` 查目標候選，因此稽核日誌的三個條件仍為精確輸入而非選擇器 | R-3 |

### 來自 MVP 降級的升級路徑

以下項目在 MVP 階段刻意降級，正式推廣時可能需要重新評估。降級未製造技術債——容器化、健康檢查、映像建置皆已就緒。

| 候選項 | MVP 現況 | 來源 |
|---|---|---|
| Kubernetes 部署與灰度發布 | Docker Compose 單機，重建時短暫停機 | D-5 |
| 生產級可觀測性 | 結構化日誌加健康輪詢，無 Prometheus 或 APM | D-5 |
| 通知收件人目錄服務 | 靜態對照，全體共用同一組 workspace webhook | Task 10.10 |
| outbox 輪詢器 | `domain_events` 表已含 status／attempts／last_error 且治理事件已寫入，但取出並派送的那一層未實作 | Task 10.10 |
